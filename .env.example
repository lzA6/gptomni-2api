# ====================================================================
# gptomni-2api 配置文件模板 (v1.0 - Omega Edition)
# ====================================================================
#
# 请将此文件重命名为 ".env" 并填入您的凭证。
#

# --- 核心安全配置 (必须设置) ---
# 用于保护您 API 服务的访问密钥。请修改为您自己的复杂密钥。
API_MASTER_KEY=sk-gptomni-2api-default-key-please-change-me

# --- 部署配置 (可选) ---
# Nginx 对外暴露的端口
NGINX_PORT=8088

# --- GPT Omni 凭证 (必须设置) ---
# 这是一个 JSON 格式的字符串列表。请将从浏览器获取的完整 Cookie 字符串放入列表中。
# 推荐配置多个账号的 Cookie 以实现轮询。
#
# 单账号示例: GPTOMNI_CREDENTIALS='["在此处粘贴您的完整 Cookie 字符串"]'
# 多账号示例: GPTOMNI_CREDENTIALS='["Cookie账号1", "Cookie账号2"]'
#
GPTOMNI_CREDENTIALS='["在此处粘贴您的完整 Cookie 字符串"]'

